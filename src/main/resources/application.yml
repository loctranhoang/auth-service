spring:
  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO

# Database configuration
# Change these settings based on your environment
# Use appropriate database dependency in pom.xml
# Example for H2 Database:
# spring:
#   datasource:
#     url: jdbc:h2:mem:testdb
#     driver-class-name: org.h2.Driver
#     username: sa
#     password: 

# Optional security settings
# security:
#   user:
#     name: user
#     password: password

# Profiles configuration
# dev: local development
# prod: production
# staging: staging environment
# local: local testing