server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stagingdb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: staginguser
    password: stagingpassword
  logging:
    level:
      org.springframework: WARN

external:
  api-url: http://staging.example.com/api