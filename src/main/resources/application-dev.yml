spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dev_user
    password: dev_password
  server:
    port: 8081
  logging:
    level:
      root: INFO
