server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: devuser
    password: devpassword
  logging:
    level:
      org.springframework: INFO

external:
  api-url: http://dev.example.com/api