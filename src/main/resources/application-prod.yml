spring:
  datasource:
    url: jdbc:mysql://prod-db:3306/auth_db
    username: prod_user
    password: prod_pass
  logging:
    level:
      root: ERROR
      com.az.auth: ERROR
  server:
    port: 80